import React from 'react';

const burgerIngredient = (props) => {
	let ingredient = null;

	switch (props.type) {
		case('bread-bottom'):
			ingrdient = <div className={}></div>
	}
	return ()
};

export default burgerIngredient;